import { FieldToObjectPipe } from './field-to-object.pipe';

describe('FieldToObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldToObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
